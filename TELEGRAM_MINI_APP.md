# Интеграция с Telegram Mini App

## 📱 Описание

Приложение полностью интегрировано с Telegram Mini App API и может запускаться как:
- 🌐 **Веб-приложение** (обычный браузер)
- 📱 **Telegram Mini App** (внутри Telegram)

## ✨ Возможности

### 1. Автоматическое определение платформы
- Приложение автоматически определяет, запущено ли оно в Telegram
- Работает корректно в обоих режимах

### 2. Данные пользователя
```typescript
const { user } = useTelegram();

// Доступные данные:
user.id              // Уникальный ID пользователя
user.username        // @username
user.first_name      // Имя
user.last_name       // Фамилия
user.language_code   // Язык (ru, en, etc.)
user.is_premium      // Премиум-подписка
user.photo_url       // URL аватара
```

### 3. Haptic Feedback (вибрация)
При взаимодействии с приложением:
- 🔘 **Клик на "Подробнее о тренере"** → легкая вибрация
- 📝 **Клик на "Записаться"** → средняя вибрация
- ✅ **Успешная запись** → вибрация успеха

### 4. Автоматическое раскрытие
Приложение автоматически раскрывается на весь экран Telegram

**Реализация:**
```typescript
// В хуке useTelegram.ts
webApp.expand();  // Раскрыть на весь экран
console.log('isExpanded:', webApp.isExpanded);  // true
```

**Проверка состояния:**
```typescript
const { isExpanded } = useTelegram();
console.log('Приложение раскрыто:', isExpanded);  // true
```

### 5. Адаптация к теме Telegram
Автоматическое применение цветовой схемы Telegram (светлая/темная тема)

## 🛠️ Технические детали

### Структура файлов

```
/index.html                      # HTML с Telegram скриптом
/src/lib/telegram.ts            # TypeScript типы для Telegram API
/src/hooks/useTelegram.ts       # React хук для работы с Telegram
/src/app/App.tsx                # Главный компонент с интеграцией
```

### Хук `useTelegram`

```typescript
const {
  tg,                   // Объект Telegram WebApp
  user,                 // Данные пользователя
  isReady,              // Готовность API
  isTelegramWebApp,     // Флаг: запущено в Telegram?
  colorScheme,          // 'light' | 'dark'
  platform,             // iOS, Android, web, etc.
  
  // Методы
  showMainButton,       // Показать главную кнопку
  hideMainButton,       // Скрыть главную кнопку
  sendData,             // Отправить данные боту
  close,                // Закрыть приложение
  hapticFeedback,       // Тактильный отклик
  showBackButton,       // Показать кнопку "Назад"
  hideBackButton,       // Скрыть кнопку "Назад"
} = useTelegram();
```

### Пример использования

```typescript
import { useTelegram } from '../hooks/useTelegram';

function MyComponent() {
  const { user, hapticFeedback, showMainButton } = useTelegram();

  const handleClick = () => {
    // Вибрация
    hapticFeedback('medium');
    
    // Показать главную кнопку
    showMainButton('Готово', () => {
      console.log('Кнопка нажата!');
    });
  };

  return (
    <div>
      {user && <p>Привет, {user.first_name}!</p>}
      <button onClick={handleClick}>Кликни меня</button>
    </div>
  );
}
```

## 🚀 Как создать Telegram Mini App

### Шаг 1: Создать бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота

### Шаг 2: Создать Mini App

1. Отправьте команду `/newapp` в [@BotFather](https://t.me/BotFather)
2. Выберите вашего бота
3. Введите название приложения
4. Загрузите иконку (640x640 px)
5. Введите описание
6. Загрузите GIF/видео демонстрацию (опционально)
7. **Введите URL вашего приложения** (например: `https://your-domain.com`)

### Шаг 3: Настройка веб-сервера

Для работы Telegram Mini App требуется **HTTPS**:

#### Вариант A: Ngrok (для разработки)
```bash
# Установите ngrok
npm install -g ngrok

# Запустите приложение локально
npm run dev

# В другом терминале запустите ngrok
ngrok http 5173

# Скопируйте HTTPS URL (например: https://abc123.ngrok.io)
# Используйте его в настройках Mini App
```

#### Вариант B: Production (Vercel, Netlify, etc.)
```bash
# Деплой на Vercel
npm install -g vercel
vercel

# Скопируйте production URL
# Используйте его в настройках Mini App
```

### Шаг 4: Тестирование

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку меню (три полоски) → "Запустить приложение"
3. Приложение откроется внутри Telegram
4. Проверьте консоль браузера (DevTools в Telegram Desktop)

## 🔧 Дополнительные возможности

### MainButton (главная кнопка)

```typescript
const { showMainButton, hideMainButton } = useTelegram();

// Показать кнопку
showMainButton('Забронировать', () => {
  console.log('Пользователь нажал кнопку!');
});

// Скрыть кнопку
hideMainButton();
```

### BackButton (кнопка "Назад")

```typescript
const { showBackButton, hideBackButton } = useTelegram();

// Показать кнопку "Назад"
showBackButton(() => {
  console.log('Пользователь нажал "Назад"');
  // Вернуться на предыдущий экран
});

// Скрыть кнопку
hideBackButton();
```

### Отправка данных боту

```typescript
const { sendData } = useTelegram();

// Отправить данные боту (строка или объект)
sendData({ 
  action: 'booking',
  trainerId: 123,
  userId: 456 
});
```

Эти данные можно обработать в боте:

```python
# Python (aiogram)
@dp.message_handler(content_types=['web_app_data'])
async def handle_web_app_data(message: types.Message):
    data = json.loads(message.web_app_data.data)
    print(f"Получены данные: {data}")
    await message.answer("Данные получены!")
```

### Cloud Storage (хранение данных)

```typescript
const tg = window.Telegram.WebApp;

// Сохранить данные
tg.CloudStorage.setItem('favorite_trainer', 'Софья Карбышева', (error, success) => {
  if (success) {
    console.log('Данные сохранены!');
  }
});

// Получить данные
tg.CloudStorage.getItem('favorite_trainer', (error, value) => {
  if (value) {
    console.log('Любимый тренер:', value);
  }
});
```

## 📊 Доступные события

```typescript
const tg = window.Telegram.WebApp;

// Изменение размера окна
tg.onEvent('viewportChanged', () => {
  console.log('Размер окна изменился');
});

// Изменение темы
tg.onEvent('themeChanged', () => {
  console.log('Тема изменена:', tg.colorScheme);
});

// Клик по MainButton
tg.MainButton.onClick(() => {
  console.log('Главная кнопка нажата!');
});

// Клик по BackButton
tg.BackButton.onClick(() => {
  console.log('Кнопка "Назад" нажата!');
});
```

## 🎨 Цветовая схема

Приложение автоматически адаптируется к теме Telegram:

```typescript
const { colorScheme, tg } = useTelegram();

// colorScheme: 'light' | 'dark'
console.log('Текущая тема:', colorScheme);

// Доступные цвета темы:
tg.themeParams.bg_color           // Фон
tg.themeParams.text_color         // Текст
tg.themeParams.hint_color         // Подсказки
tg.themeParams.link_color         // Ссылки
tg.themeParams.button_color       // Кнопки
tg.themeParams.button_text_color  // Текст кнопок
```

## 🐛 Отладка

### В Telegram Desktop
1. Откройте Mini App
2. Нажмите `Ctrl+Shift+I` (Windows/Linux) или `Cmd+Option+I` (Mac)
3. Откроется DevTools с консолью

### В мобильном Telegram
1. Используйте `console.log()` для отладки
2. Данные будут видны в Telegram Desktop DevTools
3. Или используйте [Eruda](https://github.com/liriliri/eruda) для отладки на мобильных:

```html
<!-- В index.html -->
<script src="https://cdn.jsdelivr.net/npm/eruda"></script>
<script>eruda.init();</script>
```

## 📝 Полезные ссылки

- 📚 [Официальная документация Telegram Mini Apps](https://core.telegram.org/bots/webapps)
- 🤖 [@BotFather](https://t.me/BotFather) - создание ботов
- 💡 [Примеры Mini Apps](https://github.com/telegram-mini-apps)
- 🔧 [TypeScript типы](https://github.com/telegram-mini-apps/types)

## ❓ FAQ

### Почему приложение не работает в Telegram?

1. Убедитесь, что используется **HTTPS** (не HTTP)
2. Проверьте, что URL правильно настроен в BotFather
3. Проверьте консоль на наличие ошибок

### Как протестировать локально?

Используйте **ngrok** для создания HTTPS туннеля к локальному серверу.

### Можно ли использовать приложение вне Telegram?

Да! Приложение корректно работает как обычный веб-сайт.

### Как получить данные пользователя?

```typescript
const { user } = useTelegram();
console.log(user?.id, user?.username);
```

### Как отправить данные боту?

```typescript
const { sendData } = useTelegram();
sendData({ action: 'booking', data: '...' });
```

## 🎯 Следующие шаги

Возможные улучшения:

1. **Персонализация** - сохранение избранных тренеров через CloudStorage
2. **Уведомления** - отправка напоминаний через бота
3. **Оплата** - интеграция Telegram Payments
4. **Чат с тренером** - прямая связь через Telegram
5. **История записей** - хранение в CloudStorage или на сервере