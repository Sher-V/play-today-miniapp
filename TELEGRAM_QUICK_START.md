# Telegram Mini App - Быстрый старт

## ⚡ Краткая инструкция

### Что уже сделано ✅

Приложение **полностью готово** для работы в Telegram Mini App:

```typescript
// При запуске автоматически:
tg.expand();  // ✅ Раскрытие на весь экран
tg.ready();   // ✅ Уведомление о готовности

// Доступны данные пользователя:
const user = tg.initDataUnsafe.user;
console.log(user.id);        // ID пользователя
console.log(user.username);  // @username
```

### Быстрый запуск

#### 1. Создайте бота (1 минута)

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям

#### 2. Создайте Mini App (2 минуты)

1. Отправьте `/newapp` в [@BotFather](https://t.me/BotFather)
2. Выберите бота
3. Введите название: `Теннисные группы`
4. Загрузите иконку (640x640 px)
5. **Введите URL:** `https://your-app-url.com`

#### 3. Тестирование локально (ngrok)

```bash
# Установите ngrok
npm install -g ngrok

# Запустите приложение
npm run dev

# В другом терминале
ngrok http 5173

# Скопируйте HTTPS URL и обновите в @BotFather
```

#### 4. Откройте в Telegram

1. Откройте вашего бота
2. Меню → "Запустить приложение"
3. ✅ Готово!

## 🎯 Что работает

### Автоматически

- ✅ Раскрытие на весь экран (`tg.expand()`)
- ✅ Получение данных пользователя
- ✅ Приветствие: "Привет, Имя! 👋"
- ✅ Адаптация к теме Telegram

### Haptic Feedback (вибрация)

- 🔘 Легкая - клик "Подробнее"
- 📝 Средняя - клик "Записаться"  
- ✅ Успех - отправка заявки

### Проверка в консоли

Откройте DevTools (`Ctrl+Shift+I` в Telegram Desktop):

**В Telegram:**
```javascript
// Должно быть в консоли:
📱 Раскрываем Mini App на весь экран...
✅ Mini App раскрыт. isExpanded: true
✅ Telegram User ID: 123456789
✅ Telegram Username: @username
✅ Telegram User: { id: 123456789, ... }
```

**В браузере:**
```javascript
// Должно быть в консоли:
🌐 Режим браузера: Приложение работает как обычный веб-сайт
💡 Для запуска как Telegram Mini App откройте через бота
```

## 📁 Ключевые файлы

- `/index.html` - Telegram скрипт
- `/src/hooks/useTelegram.ts` - Хук Telegram API
- `/src/lib/telegram.ts` - TypeScript типы
- `/src/app/App.tsx` - Интеграция

## 🔧 Пример использования

```typescript
import { useTelegram } from '../hooks/useTelegram';

function MyComponent() {
  const { 
    user,              // Данные пользователя
    hapticFeedback,    // Вибрация
    showMainButton,    // Главная кнопка
    isExpanded         // Статус раскрытия
  } = useTelegram();

  const handleClick = () => {
    hapticFeedback('medium');
    console.log('User:', user?.first_name);
  };

  return (
    <button onClick={handleClick}>
      Привет, {user?.first_name || 'Гость'}!
    </button>
  );
}
```

## 📚 Полная документация

- [`TELEGRAM_MINI_APP.md`](./TELEGRAM_MINI_APP.md) - Подробное руководство
- [`TELEGRAM_EXPAND_EXAMPLE.md`](./TELEGRAM_EXPAND_EXAMPLE.md) - Примеры `tg.expand()`

## ⚠️ Важно

1. **HTTPS обязателен** - используйте ngrok или production URL
2. **Проверяйте консоль** - все логи в DevTools
3. **Тестируйте в Desktop** - удобнее отладка

## 🚀 Production deploy

```bash
# Vercel
npm install -g vercel
vercel

# Netlify
npm install -g netlify-cli
netlify deploy --prod

# Обновите URL в @BotFather
```

---

✅ **Готово!** Приложение работает как Mini App и как обычный сайт.